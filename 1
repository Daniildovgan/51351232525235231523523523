#include <iostream>
#include <algorithm>

int main() {
    int M, N;
    std::cin >> M >> N;
    
    int A[M], B[N];
    for (int i = 0; i < M; ++i) {
        std::cin >> A[i];
    }
    for (int i = 0; i < N; ++i) {
        std::cin >> B[i];
    }
    
    int C[M], C_size = 0;
    for (int i = 0; i < M; ++i) {
        if (std::find(B, B + N, A[i]) == B + N) {
            if (std::find(C, C + C_size, A[i]) == C + C_size) {
                C[C_size++] = A[i];
            }
        }
    }
    
    for (int i = 0; i < C_size; ++i) {
        std::cout << C[i] << " ";
    }
    std::cout << std::endl;
    
    return 0;
}
