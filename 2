#include <iostream>
#include <algorithm>
#include <vector>

int main() {
    int M, N;
    std::cin >> M >> N;
    
    int A[M], B[N];
    for (int i = 0; i < M; ++i) {
        std::cin >> A[i];
    }
    for (int i = 0; i < N; ++i) {
        std::cin >> B[i];
    }
    
    std::vector<int> C;
    
    for (int i = 0; i < M; ++i) {
        if (std::find(B, B + N, A[i]) == B + N && std::find(C.begin(), C.end(), A[i]) == C.end()) {
            C.push_back(A[i]);
        }
    }
    
    for (int i = 0; i < N; ++i) {
        if (std::find(A, A + M, B[i]) == A + M && std::find(C.begin(), C.end(), B[i]) == C.end()) {
            C.push_back(B[i]);
        }
    }
    
    for (int num : C) {
        std::cout << num << " ";
    }
    std::cout << std::endl;
    
    return 0;
}
