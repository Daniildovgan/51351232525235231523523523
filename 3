#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int M, N;
    std::cin >> M >> N;
    
    int A[M], B[N];
    for (int i = 0; i < M; ++i) {
        std::cin >> A[i];
    }
    for (int i = 0; i < N; ++i) {
        std::cin >> B[i];
    }
    
    std::vector<int> C;

    for (int i = 0; i < M; ++i) {
        if (std::find(C.begin(), C.end(), A[i]) == C.end()) {
            C.push_back(A[i]);
        }
    }
    
    for (int i = 0; i < N; ++i) {
        if (std::find(C.begin(), C.end(), B[i]) == C.end()) {
            C.push_back(B[i]);
        }
    }
    
    for (int num : C) {
        std::cout << num << " ";
    }
    std::cout << std::endl;
    
    return 0;
}
